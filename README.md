# Official demo page for UniCue

## One-click local run

Run `./start_demo.command` inside `UniCue-demo`, or double-click `start_demo.command` in Finder.
The page will open at `http://127.0.0.1:8000/` by default.
